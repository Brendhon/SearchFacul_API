## Segredo para a decriptografia

**Crie um arquivo** na pasta utils com as seguintes características:
* Nome do arquivo: `Secret.json`
* Formato:
    ```json
    {
        "secret": "KEY"
    }
    ```
* "secret" deve conter **32** caracteres